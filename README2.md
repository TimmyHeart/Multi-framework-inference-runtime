### **Chi tiết cách implement key rotation & fallback trong Butler.**
* Grateful fallback for each API model of gemini, put them to sleep within 250ms if the calling was failing to prevent crash and rate-limit hit, retry every model possible, if one failed then instantly put it into sleep for 250ms and try that model again with another key if fail again then swith to another model right away in a loop, if all gemini failed then instantly swap into groq with 3 model and doing the same method with sleep and swap. Also there's a function to blacklist models for 24hours if request peek per day/token of day has been reached.

### **Conversation memory + edit message handling hoạt động thế nào.** 
* At first, brutally removed 2000 messages cache to help prevent OOM on only 100 MB of ram on discloud so that people could edit their message perfectly without any issue but then improved it down only to 500 messages brutally removed but with 20 messages cache force remember so that the brutal remove cannot touch that 20 message cache. Tried to located the cache on hardisk but the speed and cache leak is just a bigger gap than just storage them on RAM so that was why the 6mb idle and 12MB run-time was born and all is on just 12MB of RAM with 100mb of ram allowed but actually never reached that 13MB

### **Walkthrough một đoạn code khó trong Wan 2.2 pipeline** 
* Why I did that without OOM and why I used shard memory instead of normal usaged and optimize ? It is interesting progress, putting it into shards allowing us to virtualize the smart-memory just like ComfyUI but higher optimization of customable numbers ( 4GB of shard of smaller or bigger ), so that way it could prevent OOM and prevent bandwidth limit of the PCIE 1.1 x16 rather than just keep the normal flow all of the time which gonna choke and reveal the true bottleneck. Also doing shards with DMA/pinned memories also have to increase the data flow without worrying the data might be touched by any other procress/works/software. It is like telling the computer : HEY THAT BELONGS TO WAN2.2 14B ! DO NOT TOUCH IT! => for pinned and the DMA helped that method to be able to transfer the data smoothly directly into the CUDA CORE which gonna bypass the bandwidth issues.

### **Trade-off wthen you use AI to code** 
* Pain in the ass but also like 2 faces of the card. AIs are good to be used as an tool but sometimes they have that " ảo giác " which sometime they believe that gaslightning that they've created themselves and make users believe into that too, something they would just gonna say: hey, that's imposssible to do, do another way. Sometimes they would look directly into the works and says things right at first until they have reached the context and began to " ảo giác " again. So gotta use it as a tool rather than let it control the user
